# The age recipient is supplied explicitly by tools/init-secrets. Keeping it out
# of this file lets each sandbox operator use their own age identity.
creation_rules:
  - path_regex: deployment/secrets/.*\.enc\.env$
