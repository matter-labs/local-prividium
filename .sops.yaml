# The initialization CLI supplies the age recipient explicitly. Keeping it out
# of this file allows either the generated local identity or an operator override.
creation_rules:
  - path_regex: deployment/secrets/.*\.enc\.env$
